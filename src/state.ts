import { Modal } from './library/modal'
import { getInstance, getOrCreateInstance, removeInstance } from './library/store'

export const dntModals = {
  Modal,
  getInstance,
  getOrCreateInstance,
  removeInstance,
}
